# Device identity files (local only)

Per-device identity: `{SERIAL}.user.txt`

Production provisioning is handled by `../../factory/provision.py` at repo root.

See [../../factory/README.md](../../factory/README.md).
