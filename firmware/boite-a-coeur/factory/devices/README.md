# Device identity files (local only)

One file per physical device, named `{serial_number}.user.txt`.

These files are **not** uploaded to FFAT in normal operation. Config lives in **NVS** after first boot.

Use only for one-shot NVS migration (see `../README.md`).
